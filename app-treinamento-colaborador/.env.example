# IP da máquina que roda o backend (apps/backend), na MESMA rede Wi-Fi do
# celular físico usado para testar via Expo Go. "localhost"/"127.0.0.1" não
# funcionam a partir do app rodando no celular.
#
# Para descobrir o IP local no Windows: ipconfig (procure o adaptador Wi-Fi).
EXPO_PUBLIC_API_URL=http://192.168.45.20:8000
